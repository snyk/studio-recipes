# Secure At Inception (SAI)

Automatically scan new or modified code for security vulnerabilities as the AI generates it. This implements the "shift-left" security principle -- catching issues at the moment of creation.

## Overview

Secure At Inception ensures that every piece of AI-generated code is security-scanned before the session ends. Multiple implementation approaches are available, covering different coding assistants and enforcement models.
